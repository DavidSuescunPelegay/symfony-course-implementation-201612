{% extends 'frontend/layout.html.twig' %}

{% block body %}

    <div style="text-align: center; margin-top: 200px">
        <img id="logo" class="brand-img" src="{{ asset('bundles/appBundle/') }}frontend/images/logo.png" width="130px" alt="">
    </div>

    <p style="text-align: center">Enter your login details</p>

    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <form method="post" action="{{ path("login_check") }}" id="form_login¡">
                <div class="form-group">
                    <input type="text" class="form-control texto-login" id="username" name="_username" placeholder="Email" value="{{ last_username }}" required>
                </div>
                <div class="form-group">
                    <input type="password" class="form-control texto-login" id="password" name="_password" placeholder="Password" required>
                </div>
                <button type="submit" class="btn btn-primary btn-block texto-login">Log in</button>
            </form>
        </div>
    </div>

{% endblock %}

